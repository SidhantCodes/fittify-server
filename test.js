// Load environment variables from .env file
require('dotenv').config();

// Log the MongoDB URI
console.log('MongoDB URI:', process.env.MONGODB_URI);

// You can add more code here to test other environment variables if needed
